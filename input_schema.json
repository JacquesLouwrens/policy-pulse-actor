{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Policy Pulse Actor Input",
    "type": "object",
    "properties": {
        "url": {
            "type": "string",
            "title": "Policy URL",
            "description": "Enter the URL of the policy page you want to fetch"
        }
    },
    "required": ["url"]
}
